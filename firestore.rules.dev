rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT / EMULATOR RULES - OPEN ACCESS
    // Use this file only when running the local emulator for development and testing.
    // DO NOT deploy these rules to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
