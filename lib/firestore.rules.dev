rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ⚠️ Només per a desenvolupament local
      allow read, write: if true;
    }
  }
}
